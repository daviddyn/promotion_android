<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.promotion">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppTheme">

        <activity android:name=".activities.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.LoginRegisterActivity" />
        <activity android:name=".activities.SelectRoleActivity" />
        <activity android:name=".activities.ViewRoleExamineStateActivity" />
        <activity android:name=".activities.CreateRoleActivity" />
        <activity android:name=".activities.SearchSelectItemActivity" />
        <activity android:name=".activities.WebActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.ProjectActivity" />
        <activity android:name=".activities.MemberActivity" />
        <activity android:name=".activities.ChangePasswordActivity" />
        <activity android:name=".activities.MemberInfoActivity" />
        <activity android:name=".activities.ExamineUserInfoActivity" />
        <activity android:name=".activities.SearchMemberActivity"
            android:theme="@style/FadeActivityTheme"/>
        <activity android:name=".activities.SearchMemberResultActivity" />

    </application>

</manifest>